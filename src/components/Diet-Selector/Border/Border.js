import React from 'react';
import { View } from 'react-native';

const Border = () => (
  <View
    style={{
      borderBottomColor: '#ddd',
      borderBottomWidth: 1,
    }}
  />
)

export default Border;